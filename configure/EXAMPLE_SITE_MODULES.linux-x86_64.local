### Uncomment the lines for the drivers that should be built

### ADPointGrey cannot be built on systems without libc 2.14
ADPOINTGREY=$(AREA_DETECTOR)/ADPointGrey

### ADEiger cannot be built on 32-bit Centos7 because libzmq.so is not available
ADEIGER=$(AREA_DETECTOR)/ADEiger

### ADGenICam cannot be built without C++11
ADGENICAM=$(AREA_DETECTOR)/ADGenICam

### ADAravis is only configured to be built on this system
ADARAVIS=$(AREA_DETECTOR)/ADAravis
