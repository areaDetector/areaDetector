# This file is optionally included from RELEASE_LIBS.local and RELEASE_PRODS.local.
# It can override the definition of EPICS_BASE for a specific EPICS target architecture

EPICS_BASE=H:/epics-devel/base-7.0.3

# If CONFIG_SITE.local defines WITH_PVA=YES and EPICS_BASE is prior to 7.0 then define the path here
# If using EPICS_BASE 7.0 or later the PVA files are in base itself and no additional definitions are needed.
#PVA=H:/epics-devel/epicsV4/EPICS-CPP-4.6.0
